/// Allowed actions enum: fill, empty, transfer
enum WaterAction {
  /// Fills the Jug from the Ocean
  fill,

  /// Empty the Jug in the Ocean
  empty,

  /// Transfer water from one Jug to another
  transfer,
}
